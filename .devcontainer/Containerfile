FROM node:22-slim

# Cài đặt các công cụ hệ thống cần thiết
RUN apt-get update && apt-get install -y \
    git \
    curl \
    wget \
    jq \
    python3 \
    python3-pip \
    unzip \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Cài đặt pnpm và opencode-ai
RUN npm install -g pnpm opencode-ai

# Cài đặt RTK (build time để tiết kiệm thời gian setup)
RUN curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh

# Đảm bảo quyền sở hữu thư mục làm việc cho user node
RUN mkdir -p /workspace && chown -R node:node /workspace

WORKDIR /workspace

ENV PAPERCLIP_HOME=/workspace/paperclip-data
ENV XDG_CONFIG_HOME=/workspace/.config
ENV LANG=C.UTF-8
ENV LC_ALL=C.UTF-8

USER node
